{
  "devCommand": "uvicorn api.server:app --host 0.0.0.0 --port 3000",
  "builds": [
    {
      "src": "src/api/server.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/api/server.py"
    }
  ]
}
